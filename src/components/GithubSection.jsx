// This component is now deprecated after integrating GitHub link into Hero and card in WorkShowcase.
// Keeping file temporarily to avoid broken imports if any stale references exist. Can be safely deleted later.
const GithubSection = () => null;
export default GithubSection;
